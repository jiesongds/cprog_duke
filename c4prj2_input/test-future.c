#include <stdio.h>
#include <stdlib.h>
#include "future.h"
#include "deck.h"
#include "cards.h"

int main() {
    future_cards_t * fc;
    fc = malloc(sizeof(*fc));

    deck_t * h1; 
    h1 = malloc(*h1); 
    h1->cards = NULL;
    h1->n_cards = 0; 
    add_empty_card(h1);
    add_empty_card


}